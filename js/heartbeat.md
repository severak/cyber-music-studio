# heartbeat.js

simple webaudio library

## common interface

- `var synth = hb.SynthName(out)`
- `synth.param(name, val)`
- `synth.noteOn(nn, vv)`
- `synth.noteOff(nn)`
- `synth.plan(at, nn, vv, dur)`
- `synth.panic()`

TODO - dokumentace